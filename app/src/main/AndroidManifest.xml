<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.metromate">


<application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MetroMate">

        <!-- SplashActivity를 LAUNCHER로 설정 -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.MetroMate.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity 설정 -->
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".DataInitActivity" />
        <!--<activity android:name=".PathFinding.AStarTestActivity" />
        <activity android:name=".PathFinding.PathFindingActivity" />
        <activity android:name=".fare.FareCalculationActivity" />
-->

    </application>
</manifest>